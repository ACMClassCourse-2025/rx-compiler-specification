# Unit type

```grammar,types
TupleType -> `(` `)`
```

The unit type is written `()`, and its value is described by [TupleExpression].

Its valid data uses and the function/control-flow result exception are defined by the [zero-sized-data rules](../undefined-behavior.md#zero-sized-data).
